#include <iostream>
#include "my_functions.h"

void plot()
{
    using namespace std;
    TFile * inFile = new TFile("get_scalar_charge.root");
    TFile * outFile = new TFile("plot.root");
    outFile->cd();


}
